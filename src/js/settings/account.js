var settingsModule = settingsModule || angular.module('aio.settings', []);

settingsModule.factory('Account', ['Constants', 'Config', function(C, Config){
    var setup = function(){
        // setup apps
    }
}]);